require 'spec_helper'

describe Ng::Bank::Parser do
  it 'has a version number' do
    expect(Ng::Bank::Parser::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
